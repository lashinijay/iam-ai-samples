---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent2-core
  namespace: {{ .Values.billingInvestigationAgentCell.namespace }}
  labels:
    {{- include "cell-based-agent.labels" . | nindent 4 }}
    app.kubernetes.io/name: agent2-core
    app.kubernetes.io/component: agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent2-core
  namespace: {{ .Values.billingInvestigationAgentCell.namespace }}
  labels:
    {{- include "cell-based-agent.labels" . | nindent 4 }}
    app.kubernetes.io/name: agent2-core
    app.kubernetes.io/component: agent
spec:
  replicas: {{ .Values.billingInvestigationAgentCell.core.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: agent2-core
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent2-core
        app.kubernetes.io/component: agent
    spec:
      serviceAccountName: agent2-core
      securityContext:
        {{- include "cell-based-agent.podSecurityContext" . | nindent 8 }}
      containers:
        - name: agent2-core
          image: {{ .Values.billingInvestigationAgentCell.core.image }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: EGRESS_GW_URL
              value: "http://egress-gw.{{ .Values.billingInvestigationAgentCell.namespace }}.svc.cluster.local:8080"
            - name: INTERCELL_GW_URL
              value: "http://intercell-gw.{{ .Values.billingInvestigationAgentCell.namespace }}.svc.cluster.local:8080"
            - name: LOG_LEVEL
              value: "INFO"
            - name: AGENT_A2A_URL
              value: "http://intercell-gw.{{ .Values.customerServiceAssistantAgentCell.namespace }}.svc.cluster.local:8080/a2a/"
            - name: AGENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.billingInvestigationAgentCell.core.oauthCredentialsSecretName }}
                  key: AGENT_ID
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
        - name: auth-sidecar
          image: {{ .Values.billingInvestigationAgentCell.core.authSidecarImage }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: auth
          env:
            - name: ASGARDEO_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.billingInvestigationAgentCell.core.oauthCredentialsSecretName }}
                  key: ASGARDEO_BASE_URL
            - name: CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.billingInvestigationAgentCell.core.oauthCredentialsSecretName }}
                  key: CLIENT_ID
            - name: AGENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.billingInvestigationAgentCell.core.oauthCredentialsSecretName }}
                  key: AGENT_ID
            - name: AGENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.billingInvestigationAgentCell.core.oauthCredentialsSecretName }}
                  key: AGENT_SECRET
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: agent2-core
  namespace: {{ .Values.billingInvestigationAgentCell.namespace }}
  labels:
    {{- include "cell-based-agent.labels" . | nindent 4 }}
    app.kubernetes.io/name: agent2-core
    app.kubernetes.io/component: agent
spec:
  selector:
    app.kubernetes.io/name: agent2-core
  ports:
    - port: 8080
      targetPort: 8080
      name: http
